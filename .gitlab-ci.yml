# S3 미리보기 배포는 GitHub Actions로 옮겼다.
# `.github/workflows/deploy-s3.yml` — origin-hub(https://github.com/yangguki/score-up.git)의 main push.
# 시놀로지 GitLab에 push해도 잡을 만들지 않는다 (Runner 없음).

workflow:
  rules:
    - when: never
