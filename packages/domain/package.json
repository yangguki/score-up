{
  "name": "@score-up/domain",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "node --import tsx --test src/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
